import { QuestionAnswer } from './types';

const hardQuestions: QuestionAnswer[] = [
  // 🧠 Pure JavaScript
  {
    question: "Объясни, как работает event loop в JavaScript, включая call stack, task queue и microtasks.",
    answer: "Event Loop в JavaScript - это механизм, который позволяет выполнять асинхронный код. Call Stack хранит текущий контекст выполнения и отслеживает функции, которые выполняются в данный момент. Task Queue содержит callback-функции, которые будут выполнены после текущего стека вызовов. Microtasks (Promise.then/catch/finally, queueMicrotask) имеют приоритет над обычными tasks и выполняются сразу после текущего стека вызовов. Event Loop постоянно проверяет стек вызовов и очереди задач, обеспечивая правильный порядок выполнения."
  },
  {
    question: "Чем отличаются синхронные и асинхронные итераторы? Приведи пример реального кейса использования async generators.",
    answer: "Синхронные итераторы (Symbol.iterator) возвращают {value, done} сразу, асинхронные (Symbol.asyncIterator) возвращают Promise этого объекта. Async генераторы идеальны для потоковой обработки данных: чтение больших файлов чанками, пагинированные API-запросы, обработка событий из стримов. Пример: async function* fetchPaginatedData() { let page = 1; while(true) { const response = await fetch(`/api/data?page=${page}`); const data = await response.json(); if(data.length === 0) break; yield* data; page++; } }"
  },
  {
    question: "Как работает прототипное наследование? Чем __proto__ отличается от prototype?",
    answer: "В JavaScript объекты наследуются через прототипы. Каждый объект имеет внутреннее свойство [[Prototype]] (доступное как __proto__), указывающее на прототип объекта. При поиске свойства сначала проверяется сам объект, затем его прототип, и так далее по цепочке. prototype - это свойство функций-конструкторов, которое определяет, какой объект будет использоваться как прототип для созданных с помощью new экземпляров. __proto__ указывает на прототип конкретного объекта, а prototype определяет, что будет использоваться как прототип для создаваемых объектов."
  },
  {
    question: "Что делает Object.defineProperty и зачем использовать геттеры/сеттеры в реальном коде?",
    answer: "Object.defineProperty позволяет точно определить свойство объекта и его атрибуты (writable, enumerable, configurable). Геттеры/сеттеры в реальном коде используются для: 1) Валидации данных при установке, 2) Вычисляемых свойств, 3) Инкапсуляции, 4) Логирования доступа к свойствам, 5) Ленивой инициализации ресурсоёмких свойств, 6) Реализации реактивности (как в Vue.js). Это позволяет контролировать доступ к данным и добавлять дополнительную логику при чтении/записи свойств."
  },
  {
    question: "Почему typeof null === 'object'? Как исторически это появилось и какие баги это может вызвать?",
    answer: "typeof null === 'object' - это известный баг JavaScript, который никогда не был исправлен из-за обратной совместимости. В ранней реализации JS все значения представлялись 32-битным числом, где первые 3 бита указывали на тип. Тип 000 означал объект, а null был просто нулевым указателем (0x00), поэтому определялся как объект. Это может вызвать баги при проверках типов, например, когда нужно отличить null от объектов. Правильная проверка: value !== null && typeof value === 'object'."
  },
  {
    question: "Как работает замыкание? И как можно случайно устроить memory leak при его использовании?",
    answer: "Замыкание - это функция, которая запоминает лексическое окружение, где она была создана, даже если выполняется вне этого окружения. Memory leak может произойти, когда функция хранит ссылку на большие объекты или DOM-элементы, которые больше не нужны, но не могут быть собраны сборщиком мусора из-за замыкания. Примеры утечек: создание обработчиков событий без их удаления, циклические ссылки, хранение данных в глобальных или долгоживущих замыканиях, бесконтрольное использование setInterval/setTimeout."
  },
  {
    question: "Опиши процесс хостинга переменных и функций. В чём разница между function declaration и expression?",
    answer: "Хостинг - процесс, при котором объявления переменных и функций перемещаются в начало своей области видимости. Function declaration (function foo() {}) поднимается полностью с телом функции, поэтому их можно вызывать до объявления. Function expression (const foo = function() {}) поднимается только сама переменная без присваивания значения. Переменные var поднимаются со значением undefined, а let/const поднимаются в Temporal Dead Zone без инициализации и вызывают ошибку при обращении до объявления."
  },
  {
    question: "Что такое Temporal Dead Zone и как это связано с let/const?",
    answer: "Temporal Dead Zone (TDZ) - это период времени от входа в область видимости, где объявлена переменная с помощью let/const, до момента её фактического объявления в коде. В TDZ переменная существует, но не инициализирована, и любая попытка доступа к ней вызовет ReferenceError. Это отличает let/const от var, которые инициализируются как undefined. TDZ сделан для выявления логических ошибок при использовании переменных до их объявления и обеспечения работы const (константа не может быть undefined, а потом получить значение)."
  },
  {
    question: "Как работает движок JS (например, V8): JIT, интерпретация, оптимизация и деоптимизация кода?",
    answer: "Современные движки JS используют многоуровневую компиляцию: 1) Парсинг кода в AST, 2) Быстрая интерпретация с базовой оптимизацией (Ignition в V8), 3) Анализ и профилирование горячего кода, 4) JIT-компиляция часто используемого кода в оптимизированный машинный код (TurboFan в V8). Деоптимизация происходит, когда предположения оптимизатора нарушаются (например, изменение типа переменной). V8 также использует встроенный сборщик мусора, инлайнинг функций, удаление мёртвого кода и другие оптимизации для ускорения выполнения."
  },
  {
    question: "Что будет, если в try/catch кинуть async функцию без await? Почему иногда ошибки теряются?",
    answer: "Если в try/catch вызвать async функцию без await, то возникшее внутри неё исключение не будет поймано этим try/catch блоком, так как async функция возвращает Promise, который перейдёт в состояние rejected при ошибке. Ошибка будет потеряна, если: 1) Нет обработчика у Promise (.catch() или await в try/catch), 2) Используются цепочки Promise без финального .catch(), 3) Ошибка в событийных колбэках без try/catch, 4) Ошибки в setTimeout/setInterval без обработки. Решения: всегда использовать await с async функциями в try/catch, добавлять .catch() к Promise, использовать window.onerror или unhandledrejection."
  },
  {
    question: "Как устроены Symbols и зачем они вообще нужны, если можно использовать строки как ключи?",
    answer: "Symbols - примитивный тип данных, представляющий уникальный идентификатор. Каждый Symbol уникален, даже если создан с одинаковым описанием: Symbol('x') !== Symbol('x'). Преимущества перед строками: 1) Гарантированная уникальность ключей объекта, 2) Защита от случайного перезаписывания (символы не видны в for-in, Object.keys()), 3) Метапрограммирование через well-known symbols (Symbol.iterator, Symbol.toPrimitive), 4) Создание приватноподобных свойств до появления приватных полей #, 5) Расширение встроенных прототипов без риска конфликтов имён."
  },
  {
    question: "Что такое Proxy в JS и как его можно использовать для слежки за объектами или валидации?",
    answer: "Proxy в JS - это обёртка вокруг объекта, перехватывающая основные операции (чтение, запись, вызов). Используется для: 1) Валидации данных перед записью (через trap set), 2) Создания реактивности (Vue 3 использует Proxy для реактивных объектов), 3) Виртуализации свойств (вычисляемые или ленивые свойства), 4) Логирования доступа, 5) Защиты объектов от изменения, 6) Реализации автодополнения в IDE. Пример валидации: const validator = { set(obj, prop, value) { if (prop === 'age' && (typeof value !== 'number' || value < 0)) throw new Error('Invalid age'); obj[prop] = value; return true; } }; const person = new Proxy({}, validator);"
  },

  // 🧱 Pug (ex-Jade)
  {
    question: "Как Pug преобразуется в HTML под капотом? Как он компилируется и какие ограничения это накладывает?",
    answer: "Pug компилируется в JS-функцию, которая возвращает HTML-строку. Процесс: 1) Парсинг в AST с анализом отступов для вложенности, 2) Трансформация AST, 3) Генерация JS-кода, выполняющего конкатенацию строк HTML. Ограничения: сложность отладки из-за двухэтапной компиляции, производительность при большом количестве динамического контента, необходимость пре-компиляции для рантайма без Node.js, недостаточная типобезопасность переменных, сложности с условной вложенностью элементов."
  },
  {
    question: "Можно ли использовать mixins как HOC? Приведи пример кастомного mixin с аргументами и блоками.",
    answer: "Да, mixins в Pug можно использовать как HOC. Пример: mixin Card(title, options = {}) <div class=`card ${options.class || ''}`> <header class='card-header'> if title <h3>#{title}</h3> else block header </header> <div class='card-body'> block </div> if block.footer <footer class='card-footer'> block footer </footer> </div> Использование: +Card('Заголовок') p Содержимое или +Card() block header h3.custom Кастомный заголовок block p Содержимое block footer small Подвал карточки"
  },
  {
    question: "Как безопасно вставлять HTML в Pug, чтобы избежать XSS, но не потерять флексибильность вывода?",
    answer: "Pug по умолчанию экранирует переменные (#{var}), защищая от XSS. Для вставки HTML безопасно: 1) Использовать !{var} только для доверенных данных (прошедших санитизацию), 2) Применять санитайзеры (DOMPurify, sanitize-html) перед вставкой: !{sanitize(userHtml)}, 3) Whitelist подход: разрешать только определённые теги/атрибуты, 4) Создавать миксины, которые строят HTML из данных: +renderComment(commentData), 5) CSP-заголовки как дополнительный уровень защиты, 6) Хранить разметку в БД как JSON-структуру и рендерить её через рекурсивные миксины."
  },
  {
    question: "В чём разница между include и extend в Pug? Какой из них вызывает меньше проблем при рефакторинге?",
    answer: "include просто вставляет содержимое файла в текущее место. extend создаёт наследование шаблонов, позволяя заменять блоки в родительском шаблоне. При рефакторинге extend обычно вызывает меньше проблем, так как имеет чёткую структуру блоков и явную связь между шаблонами. Include может вызвать проблемы при изменении структуры включаемого файла, так как нет чёткого контракта между файлами. Extend лучше для создания макетов (layout), a include для компонентов и повторного использования кода."
  },
  {
    question: "Как в Pug сделать условный рендеринг блоков с fallback'ом, если переменная не определена?",
    answer: "В Pug можно реализовать условный рендеринг с fallback несколькими способами: 1) if/else: if data block else block fallback, 2) Оператор присваивания по умолчанию: - var content = data || defaultValue, 3) Тернарный оператор: #{data ? data : 'Нет данных'}, 4) Миксины с дефолтными параметрами: mixin show(content = 'Default') p= content, 5) Block + рендеринг блока: block content block fallback p Дефолтный контент. Наиболее гибкий вариант - использование именованных блоков с динамической подстановкой."
  },
  {
    question: "Как связать переменные Pug с серверной логикой в Node.js, особенно в express + pug приложениях?",
    answer: "В Express+Pug переменные передаются из сервера в шаблоны: 1) app.get('/', (req, res) => res.render('index', { title: 'Home', user: req.user })), 2) Использование res.locals для данных, доступных во всех шаблонах, 3) Middleware для автоматической передачи общих данных: app.use((req, res, next) => { res.locals.user = req.user; next(); }), 4) Передача функций-хелперов: res.render('page', { formatDate: (d) => moment(d).format('DD.MM.YYYY') }), 5) Custom-теги для работы с серверным API (для сложных кейсов). Для структурированных данных лучше создавать view-models на сервере, а не логику в шаблонах."
  },
  {
    question: "Можно ли динамически менять layout в Pug в зависимости от route? Если да — как?",
    answer: "Да, можно динамически менять layout в Pug: 1) Через передачу layout в res.render: app.get('/admin', (req, res) => res.render('admin', { layout: 'admin-layout' })), затем в шаблоне: extends #{layout || 'default-layout'}, 2) Middleware для определения layout на основе route: app.use('/admin/*', (req, res, next) => { res.locals.layout = 'admin-layout'; next(); }), 3) Условная логика в самом шаблоне: block variables - var currentLayout = 'default' extends layouts/#{currentLayout}, 4) Создать обёртку над render: function renderWithLayout(res, view, layout, data) { res.render(view, { ...data, layout }); }."
  },
  {
    question: "Почему не стоит хранить бизнес-логику в Pug-шаблоне, и как люди обычно умудряются туда её впихнуть?",
    answer: "Бизнес-логика в Pug-шаблонах плоха: сложно тестировать, дублирование кода, смешение ответственности, проблемы производительности. Но люди впихивают её через: 1) Сложные вычисления в миксинах - mixin calculateTotal(items), 2) Злоупотребление выражениями - #{items.reduce((sum, item) => sum + item.price, 0).toFixed(2)} руб., 3) Фильтрацию данных - each item in items.filter(i => i.isActive), 4) Сортировку - each category in categories.sort((a, b) => a.order - b.order), 5) Валидацию данных, 6) Трансформацию данных в шаблоне, 7) Множественные вложенные условия с бизнес-правилами. Решение: view-models на сервере, выделение логики в хелперы."
  },
  {
    question: "Как бы ты реализовал компонентную структуру с переиспользуемыми блоками без костылей и полного ада?",
    answer: "Компонентная структура в Pug без костылей: 1) Организация папок по компонентам, каждый с миксином, стилями и документацией, 2) Создание компонентного API через параметры миксинов с дефолтными значениями, 3) Использование блоков для сложной вложенности: mixin Card(title) .card h2.card-title= title .card-body block, 4) Именованные блоки для сложных компонентов: block header, block content, 5) Component-factory для генерации компонентов из JSON: +renderComponent(componentData), 6) Предкомпиляция и кеширование компонентов, 7) Инъекция зависимостей через контекст. Использовать строгие контракты данных и документировать API компонентов."
  }
];

export default hardQuestions;
